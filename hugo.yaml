title: Hugo Playground

params:
  description: |
    ESBuild's tree shaking doesn't seem to be working.

disableKinds:
  - taxonomy
  - term

outputs:
  home:
    - HTML

module:
  imports:
    - path: github.com/UtkarshVerma/hugo-modules/bootstrap-v5
