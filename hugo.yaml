title: Hugo Playground

params:
  description: |
    PostProcess edge-case where partialCached with PostProcess does not
    generate CSS.

    In this example, SCSS is post-processed in production only and CSS is only
    generated for non-production builds.

disableKinds:
  - taxonomy
  - term

outputs:
  home:
    - HTML
